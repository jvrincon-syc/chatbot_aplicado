<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Chatbot.Sst.Domain\Chatbot.Sst.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- ILogger abstraction only (no logging provider/implementation) — used to stop
         swallowing exceptions in ChatDispatchCoordinator's catch blocks. -->
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.11" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
